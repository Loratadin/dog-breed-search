const initialState = {
    breedsList: null,
    selectedBreed: null,
    error: false,
    imageUrl: require('../../assets/custom.jpg'),
    selectedBreed: null
}

const reducer = (state = initialState, action) => {
    return state;
}

export default reducer;